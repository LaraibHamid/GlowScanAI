class AppRadii {
  /// Small rounded elements (chips, badges, small containers)
  static const double small = 8;

  /// Standard corners used on most inputs & buttons
  static const double medium = 14;

  /// Large corners used for cards, modals, primary containers
  static const double large = 20;

  /// Fully rounded (circle) — used for avatars, round buttons, etc.
  static const double full = 100;
}

